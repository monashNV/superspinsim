[project]
name = "superspinsim"
version = "0.3.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
	"numba",
	"cuda-core",
	"cuda-bindings",
	"numba-cuda",
	"numpy",
	"sympy",
	"qutip"
]

[tool.setuptools.packages.find]
exclude = [
	"julia_env*",
	"docs*",
	"comparison*",
	"test*"
]
