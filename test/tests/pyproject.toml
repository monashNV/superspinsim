[project]
name = "tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
dependencies = [
    "matplotlib>=3.10.8",
    "superspinsim",
]

[tool.uv.sources]
superspinsim = { path = "../../" }
