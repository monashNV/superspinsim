[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3, <3.14"
dependencies = [
    "pogger>=0.1.15",
]

[tool.setuptools.packages.find]
exclude = [
	"tests*"
]

